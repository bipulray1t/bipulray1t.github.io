const UrlReturn = (id) => {
      if (id === '1') {
    const DemoUrl = "https://anistreamdot.netlify.app/";
    return DemoUrl;
    

  }
  else if (id === '2') {
      const DemoUrl = "https://lumina-luxe-vibe.netlify.app/";
      return DemoUrl;
    }
    else if (id === '3') {
        const DemoUrl = "https://resumebuilderprobybipulray1t.netlify.app/";
        return DemoUrl;
    }
    else if (id === '4') {
        const DemoUrl = "https://resumebuilderliteversion.netlify.app/";
        return DemoUrl;
    }
    else if (id === '5') {
        const DemoUrl = "https://admindashboardbipulray1t.netlify.app/";
        return DemoUrl;
    }
    else if (id === '6') {
        const DemoUrl = "https://luxebitebybipulray1t.netlify.app/";
        return DemoUrl;
    }
  };
  export default UrlReturn;